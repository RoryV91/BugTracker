const userGroupNames =  ["User", "Support", "Admin"]

const priorityLevels = ["Low", "Intermediate", "High"]

const statusDecriptions = ["Submitted", "Assigned", "In Progress", "In Review", "Closed"]

module.exports = {
    userGroupNames,
    priorityLevels,
    statusDecriptions
}